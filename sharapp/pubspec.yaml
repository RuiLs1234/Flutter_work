name: sharapp
description: "A new Flutter project."
publish_to: 'none' # Remove esta linha se quiseres publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  mapbox_maps_flutter: ^2.8.0
  permission_handler: ^11.0.0
  cupertino_icons: ^1.0.8
  geolocator: ^10.1.0
  image_picker: ^1.0.4
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  intl: ^0.18.1
  firebase_core: ^2.30.0
  cloud_firestore: ^4.9.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/marker.png
